[project]
name = "open223-book"
version = "0.1.0"
description = "Jupyter Book for Open223"
requires-python = ">=3.9"
dependencies = [
    "jupyter-book",
    "myst_parser",
    "pyshacl",
    "rdflib",
    "sphinxcontrib-mermaid",
    "ipykernel",
]
